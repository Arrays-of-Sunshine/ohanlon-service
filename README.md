# Systems Design Capstone
---

## Setup
**Install Packages**
`npm install`

## DB Commands
**Login**
- 'psql -d sdc -U admin'

**Load Schema**
- 'psql -d sdc -a -f schema.sql'

**Create Mock Data**
- `npm run data`

**Seed DB**
- `npm run seed`

